<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  <script src="{{constant('shopping\\Bootstrap::APP_URL')}}js/shopping.js"></script>

  <link href="{{constant('shopping\\Bootstrap::APP_URL')}}css/style.css" rel="stylesheet">
  <title>favorite</title>
</head>
<body>
  <input type="hidden" name="entry_url" value="{{constant('shopping\\Bootstrap::ENTRY_URL')}}" id="entry_url">
  <div id="wrapper">
    <div id="favorite_list">
      <p>ほしい物リスト</p>
      <p><a href="{{constant('shopping\\Bootstrap::ENTRY_URL')}}list.php">商品一覧へ戻る</a></p>
      {% if dataArr | length == 0 %}
        <p>ほしい物リストに商品は登録されていません</p>
      {% else %}
        {% for value in dataArr %}
          <div class="item">
            <ul>
              <li class="image"><img src="{{constant('shopping\\Bootstrap::ENTRY_URL')}}images/{{value.image}}" alt="{{value.item_name}}"></li>
              <li class="name">{{value.item_name}}</li>
              <li class="price">&yen;{{value.price|number_format(0, '.', ',')}}</li>
              <input type="hidden" name="item_id" id="item_id" value="{{dataArr.item_id}}">
            </ul>
          </div> 
        <div class="cart_in">
          <input type="button" name="cart_in" value="ショッピングカートへ入れる" id="cart_in">
        </div>
        {% endfor %}
      {% endif %}
    </div>
  </div>
</body>
</html>