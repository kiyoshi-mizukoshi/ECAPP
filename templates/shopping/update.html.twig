<!DOCTYPE html>
<html lang="ja">
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
  <!-- reset css -->
  <link rel="stylesheet" href="{{constant('shopping\\Bootstrap::APP_URL')}}css/reset.css">
  
  <!-- fontawesome -->
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.6.1/css/all.css">

<!-- drawer.css -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/drawer/3.2.2/css/drawer.min.css">
<!-- jquery & iScroll -->
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/iScroll/5.2.0/iscroll.min.js"></script>
<!-- drawer.js -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/drawer/3.2.2/js/drawer.min.js"></script>

  <script src="{{constant('shopping\\Bootstrap::APP_URL')}}js/shopping.js"></script>
  <script src="{{constant('shopping\\Bootstrap::APP_URL')}}js/common.js"></script>
  <link href="{{constant('shopping\\Bootstrap::APP_URL')}}css/style.css" rel="stylesheet">
  <title>会員情報変更</title>
</head>
<body class="drawer drawer--right">
{% include 'header.html.twig' %}
    <div class="regist inner">
  <div class="regist-wrap">
  <h2 class="util-title">会員情報変更</h2>
  <input type="hidden" name="entry_url" id="entry_url" value="{{constant('shopping\\Bootstrap::ENTRY_URL')}}">
  <form action="" method="post">
    <dl class="regist-list">
      <div class="regist-item">
        <dt class="regist-item-title">お名前(氏名)</dt>
        <dd class="regist-item-input"><input type="text" name="family_name" value="{{register[0].family_name}}" required>
            <input type="text" name="first_name" value="{{register[0].first_name}}" required>
            {% if errArr.family_name | length > 0 %}<br><span class="err">{{errArr.family_name}}</span>{% endif %}
            {% if errArr.first_name | length > 0 %}<br><span class="err">{{errArr.first_name}}</span>{% endif %}</dd>
        </div>
        <div class="regist-item">
        <dt class="regist-item-title">お名前(かな)</dt>
        <dd class="regist-item-input"><input type="text" name="family_name_kana" value="{{register[0].family_name_kana}}">
          <input type="text" name="first_name_kana" value="{{register[0].first_name_kana}}"></dd>
        </div>
        <div class="regist-item">
          <dt class="regist-item-title">生年月日</dt>
          <dd class="regist-item-input">            <select name="year">
            {% for index, label in yearArr %}
            <option value="{{index}}" {% if register[0].year == index %} selected {% endif %}>{{label}}</option>
            {% endfor %}
          </select>
          <select name='month'>
            {% for index, label in monthArr %}
            <option value="{{index}}"{% if register[0].month == index %} selected {% endif %}>{{label}}</option>
            {% endfor %}
          </select>
          <select name='day'>
            {% for index, label in dayArr %}
            <option value="{{index}}"{% if register[0].day == index %} selected {% endif %}>{{label}}</option>
            {% endfor %}
          </select>
          {% if errArr.year | length > 0 %}<br><span class="err">{{errArr.year}}</span>{% endif %}
          {% if errArr.month | length > 0 %}<br><span class="err">{{errArr.month}}</span>{% endif %}
          {% if errArr.day | length > 0 %}<br><span class="err">{{errArr.day}}</span>{% endif %}
          </dd>
          </div>
          <div class="regist-item">
            <dt class="regist-item-title">郵便番号</dt>

            <dd class="regist-item-input"><input type="text" name="zip1" value="{{register[0].zip1}}" id="zip1" size="3" maxlength="3" required> 
              -
              <input type="text" name="zip2" value="{{register[0].zip2}}" id="zip2" size="4" maxlength="4" required>
              <input type="button" name="address_search" value="〒から住所を入力" id="address_search">
              {% if errArr.zip1 | length > 0 %}<br><span class="err">{{errArr.zip1}}</span>{% endif %}
              {% if errArr.zip2 | length > 0 %}<br><span class="err">{{errArr.zip2}}</span>{% endif %}</dd>
            </div>
            <div class="regist-item">
              <dt class="regist-item-title">住所</dt>
              <dd class="regist-item-input"><input type="text" name="address" value="{{register[0].address}}" id="address" size="40" required>
                {% if errArr.address | length > 0 %}<br><span class="err">{{errArr.address}}</span>{% endif %}</dd>
              </div>
              <div class="regist-item">
                <dt class="regist-item-title">メールアドレス</dt>
                <dd class="regist-item-input"><input type="email" name="email" value="{{register[0].email}}" size="40" required>
                  {% if errArr.email | length > 0 %}<br><span class="err">{{errArr.email}}</span>{% endif %}</dd>
                </div>
                <div class="regist-item">
                  <dt class="regist-item-title">電話番号</dt>
                  <dd class="regist-item-input"><input type="text" name="tel1" value="{{register[0].tel1}}" size="6" maxlength="6" required> -
                    <input type="text" name="tel2" value="{{register[0].tel2}}" size="6" maxlength="6" required> -
                    <input type="text" name="tel3" value="{{register[0].tel3}}" size="6" maxlength="6" required> 
                    {% if errArr.tel1 | length > 0 %}<br><span class="err">{{errArr.tel1}}</span>{% endif %}
                    {% if errArr.tel2 | length > 0 %}<br><span class="err">{{errArr.tel2}}</span>{% endif %}
                    {% if errArr.tel3 | length > 0 %}<br><span class="err">{{errArr.tel3}}</span>{% endif %}</dd>
                  </div>
    </dl>
    <div class="util-link-wrap">
      <input type="submit" name="confirm" value="確認画面へ" class="util-link">
      <button type="submit" class="util-link"><a href="mypage.php">会員情報へ戻る</a></button>
    </div>
    </form>
    </div>
    </div>
  {% include 'footer.html.twig' %}
</body>
</html>