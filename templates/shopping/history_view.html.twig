<!DOCTYPE html>
<html lang="ja">
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
  <!-- reset css -->
  <link rel="stylesheet" href="{{constant('shopping\\Bootstrap::APP_URL')}}css/reset.css">
  
  <!-- fontawesome -->
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.6.1/css/all.css">

<!-- drawer.css -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/drawer/3.2.2/css/drawer.min.css">
<!-- jquery & iScroll -->
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/iScroll/5.2.0/iscroll.min.js"></script>
<!-- drawer.js -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/drawer/3.2.2/js/drawer.min.js"></script>

  <script src="{{constant('shopping\\Bootstrap::APP_URL')}}js/shopping.js"></script>
  <link href="{{constant('shopping\\Bootstrap::APP_URL')}}css/style.css" rel="stylesheet">
  <title>cart</title>
</head>
<body class="drawer drawer--right">
{% include 'header.html.twig' %}
  <div class="history inner">
  <h2 class="util-title">購入詳細</h2>
    <div class="history-back-wrap">
      <a href="{{constant('shopping\\Bootstrap::ENTRY_URL')}}history.php"><p class="cart-back">購入履歴一覧に戻る</p></a>
    </div>
      {% if dataArr | length == 0 %}
        <p class="history-back non" >商品はありません</p>
      {% else %}
      <div class="history-list ">
        {% for value in dataArr %}
        <article class="history-item wow fadeInUp">
          <div class="history-item-wrap">
            <div class="history-item-header">
              <div class="history-item-picture"><img src="{{constant('shopping\\Bootstrap::ENTRY_URL')}}images/{{value.image}}" alt="{{value.item_name}}"></div>
            </div>
            <div class="history-item-body">
              <p class="history-item-name">商品名: {{value.item_name}}</p>
              <h3 class="history-item-cost">価格: &yen;{{value.price|number_format(0, '.', ',')}}</h3>
              <p class="history-item-num"> 数量: {{value.num}}
            </div>
          </div>
        </article>
        {% endfor %}
      </div>
      {% endif %}
</div>

{% include 'footer.html.twig' %}
</body>
</html>