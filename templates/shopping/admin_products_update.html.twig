<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
  <script src="{{constant('shopping\\Bootstrap::APP_URL')}}js/shopping.js"></script>
  <link rel="stylesheet" href="{{constant('shopping\\Bootstrap::APP_URL')}}css/reset.css">
  <link href="{{constant('shopping\\Bootstrap::APP_URL')}}css/style.css" rel="stylesheet">  <title>Document</title>
</head>
<body>
  <div class="admin-products inner">
    <div class="contact-wrap">
      <h2 class="util-title">商品詳細・編集</h2>
      <form action="" method="post" enctype="multipart/form-data">
        <dl class="admin-products-list">
          <div class="admin-products-item">
            <dt class="admin-products-item-title"> 商品名：</dt>
            <dd class="admin-products-item-input"><input type="text" name="item_name" size="50" value="{{dataArr[0].item_name}}" required></dd>
          </div>
          <div class="admin-products-item">
            <dt class="admin-products-item-title">価格：</dt>
            <dd class="admin-products-item-input"><input type="text" name="price" id="" value="{{dataArr[0].price|number_format(0, '.', ',')}}" required></dd>
          </div>
          <div class="admin-products-item">
            <dt class="admin-products-item-title">説明文：</dt>
            <dd class="admin-products-item-input"><textarea name="detail" id="" cols="30" rows="10" value="" required>{{dataArr[0].detail}}</textarea></dd>
          </div>
          <div class="admin-products-item">
            <dt class="admin-products-item-title">画像：<br><a href="admin_products_imageupdate.php" class="admin-link">画像の変更はこちら</a></dt>
            <dd class="admin-products-item-input"><img src="{{constant('shopping\\Bootstrap::ENTRY_URL')}}images/{{dataArr[0].image}}" alt=""></dd>
          </div>

          <div class="admin-products-item">
            <dt class="admin-products-item-title">カテゴリー：</dt>
            <dd class="admin-products-item-input"><select name="ctg_id" id="" value="">
              {% for value in cateArr %}
              <option value="{{value.ctg_id}}" name="ctg_id">{{value.category_name}}</option>
              {% endfor %}
            </select></dd>
          </div>

        </dl>
        <div class="admin-products-footer">
          <input type="submit" value="変更" name="submit" class="util-link">
          <input type="button" value="削除" name="remove" id="js-remove" class="util-link">
          {{msg}}
        </div>
      </form>
    </div>
  </div>

<!-- <form action="" method="post">
  <div class="update inner">
    <h2 class="admin-title">商品情報編集</h2>
  <table>
    <tr>
      <th>商品名</th>
      <td><input type="text" name="item_name" id="" value="{{dataArr[0].item_name}}" required></td>
    </tr>
    <tr>
      <th>説明文</th>
      <td><textarea name="detail" id="" cols="30" rows="10" value="" required>{{dataArr[0].detail}}</textarea></td>
    </tr>
    <tr>
      <th>価格</th>
      <td><input type="text" name="price" id="" value="{{dataArr[0].price|number_format(0, '.', ',')}}" required></td>
    </tr>
    <tr>
      <th>画像</th>
      <td><img src="{{constant('shopping\\Bootstrap::ENTRY_URL')}}images/{{dataArr[0].image}}" alt=""></td>
    </tr>
    <tr>
      <th>カテゴリー</th>
      <td><select name="ctg_id" id="" value="">
        {% for value in cateArr %}
        <option value="{{value.ctg_id}}" name="ctg_id" {% if  value.ctg_id == dataArr[0].ctg_id %}selected{% endif %}>{{value.category_name}}</option>
        {% endfor %}
      </select></td>
    </tr>


  </table>
  <input type="submit" value="変更" name="submit">
  <input type="button" value="削除" name="remove" id="js-remove">
  {{msg}}
  <a href="admin_products_imageupdate.php">画像を変更する</a>
  </div>
</form> -->
</body>
</html>