<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="{{constant('shopping\\Bootstrap::APP_URL')}}css/style.css" rel="stylesheet">
  <title>cart</title>
</head>
<body>
  <div id="wrapper">
    <div id="cart_list">
      <p>購入確認画面</p>
      <p><a href="{{constant('shopping\\Bootstrap::ENTRY_URL')}}cart.php">カートへ戻る</a></p>
      <p>商品数：{{sumNum}}個 合計金額：&yen;{{sumPrice|number_format(0, '.', ',')}}</p>
      {% if dataArr | length == 0 %}
        <p>カートに商品は入っていません</p>
      {% else %}
      <table>
        <tr>
          <th>商品画像</th>
          <th>商品名</th>
          <th>価格</th>
          <th>個数</th>
        </tr>
        {% for value in dataArr %}
            <tr>
              <td class="image"><img src="{{constant('shopping\\Bootstrap::ENTRY_URL')}}images/{{value.image}}" alt="{{value.item_name}}"></td>
              <td class="name">{{value.item_name}}</td>
              <td class="price">&yen;{{value.price|number_format(0, '.', ',')}}</td>
              <td class="num">{{value.num}}</td>
            </tr>
        {% endfor %}
      </table>      
        <form action="" method="POST">
          <input type="submit" name="buy" value="購入する">
        </form>      
      {% endif %}
    </div>
  </div>

</body>
</html>