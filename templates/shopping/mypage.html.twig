<!DOCTYPE html>
<html lang="ja">
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
  <!-- reset css -->
  <link rel="stylesheet" href="{{constant('shopping\\Bootstrap::APP_URL')}}css/reset.css">
  
  <!-- fontawesome -->
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.6.1/css/all.css">

<!-- drawer.css -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/drawer/3.2.2/css/drawer.min.css">
<!-- jquery & iScroll -->
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/iScroll/5.2.0/iscroll.min.js"></script>
<!-- drawer.js -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/drawer/3.2.2/js/drawer.min.js"></script>

  <script src="{{constant('shopping\\Bootstrap::APP_URL')}}js/shopping.js"></script>
  <link href="{{constant('shopping\\Bootstrap::APP_URL')}}css/style.css" rel="stylesheet">
  <title>マイページ</title>
</head>
<body class="drawer drawer--right">
  {% include 'header.html.twig' %}
  <div class="section inner mypage">
    <div class="mypage-wrap">
    <h2 class="util-title">My Page</h2>
    <table class="table">
      <tr>
        <th>登録日時</th>
        <td>{{register[0].regist_date}}</td>
      </tr>
  
      <tr>
        <th>お名前（氏名)</th>
        <td>{{register[0].family_name}} {{register[0].first_name}}</td>
      </tr>
      <tr>
        <th>お名前（かな）</th>
        <td>
          {{register[0].family_name_kana}} {{register[0].first_name_kana}}
        </td>
      </tr>
      <tr>
        <th>生年月日</th>
        <td>{{register[0].year}}年 {{register[0].month}}月 {{register[0].day}}日</td>
      </tr>
      <tr>
        <th>郵便番号</th>
        <td>{{register[0].zip1}} - {{register[0].zip2}}</td>
      </tr>
      <tr>
        <th>住所</th>
        <td>{{register[0].address}}</td>
      </tr>
      <tr>
        <th>メールアドレス</th>
        <td>{{register[0].email}}</td>
      </tr>
      <tr>
        <th>電話番号</th>
        <td>{{register[0].tel1}} - {{register[0].tel2}} - {{register[0].tel3}}</td>
      </tr>
    </table>  

    <table class="link-table">
      <tr>
        <td><a href="update.php" class="util-link">会員情報編集</a></td>
        <td><a href="password.php" class="util-link">パスワード変更</a></td>
        <td><a href="history.php" class="util-link">購入履歴</a></td>
        <td><a href="delregist.php" class="util-link">退会</a></td>
      </tr>

    </table>
  </div>
  </div>
  {% include 'footer.html.twig' %}
</body>
</html>